{
  "name": "stencil-playground",
  "private": true,
  "version": "0.0.1",
  "description": "Project with component examples and tests",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll"
  },
  "dependencies": {
    "@stencil/core": "~0.18.0",
    "classnames": "^2.2.6",
    "stencil-fragment": "^1.0.1"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/classnames": "^2.2.7",
    "workbox-build": "3.4.1"
  }
}
